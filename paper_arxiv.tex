\documentclass[11pt]{article}

% ---------- NeurIPS-style preamble ------------------------------------------
\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{amsmath, amssymb}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage[capitalize]{cleveref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{titlesec}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=blue!50!black,
  urlcolor=blue!50!black,
}

\titleformat{\section}{\normalfont\large\bfseries}{\thesection}{0.6em}{}
\titleformat{\subsection}{\normalfont\normalsize\bfseries}{\thesubsection}{0.5em}{}
\titlespacing*{\section}{0pt}{1.0em}{0.4em}
\titlespacing*{\subsection}{0pt}{0.8em}{0.3em}
\titlespacing*{\paragraph}{0pt}{0.4em}{0.5em}

\setlength{\parskip}{0.25em}

% --- title block ------------------------------------------------------------
\title{\textbf{Per-Token KL Distillation Reaches a Statistically-Significant Floor on PrincipalBench}\\[2pt]
\large An Open-Weight Recipe for Principal-Loyalty in Multi-Party LLM Agents}
\author{
  Bojie Li\\
  \texttt{bojieli@gmail.com}
}
\date{}

\begin{document}
\maketitle

\begin{abstract}
\noindent Instruction-tuned LLMs deployed as agents in multi-party conversations leak their principal's information, capitulate to social pressure, and over-refuse legitimate requests. We introduce \textbf{PrincipalBench}, a 36-item multi-turn benchmark across six failure modes (leakage, capitulation, posture, authoring, sanity, moderation) and three prompt arms (plain / prompted / scaffolded), with probe-based leak detection and dual-judge harm scoring. Three engineering results: (i) a 4-rule \textbf{v4 system prompt} drives 11 of 14 frontier subjects to harm rates of $11$--$19\%$ on a full 108-grid (DeepSeek $11\%$ best, three more at $11$--$14\%$, Llama-3.1-70B / Claude-Opus / Gemini / Qwen3-32B at $16$--$17\%$, Claude-Sonnet $19\%$); GLM-4.6 is intermediate at $49\%$; PrincipalBench separately exposes that \texttt{gpt-5}, \texttt{gpt-5-mini}, and \texttt{qwen3.5-27b} \emph{intrinsically} over-refuse on these multi-party items ($55$--$72\%$ harm at the no-prompt plain arm). Inverse-scaling within OpenAI: smaller \texttt{gpt-5-nano} ($14\%$) is calibrated while larger \texttt{gpt-5} and \texttt{gpt-5-mini} over-refuse. The bimodal split also \emph{strengthens} on held-out items: GPT-5 amplifies from $71\%$ training to $93\%$ held-out, while calibrated vendors stay $\le 24\%$ (\cref{fig:heldout_xsubj}); (ii) an architectural \texttt{[READER:\,PRINCIPAL]}/\texttt{[READER:\,THIRD\_PARTY]} \textbf{sentinel} cuts reader-identity over-refusal $9/12 \!\rightarrow\! 3/12$; (iii) \textbf{per-token KL distillation} (Thinking Machines / DeepSeek-V4) of v4-prompted Qwen3-32B-AWQ into Qwen3-8B drives the student to $33/108$ harm at $n{=}113$ on-policy turn-records --- the strongest 8B-distillation result in our study, and the first 8B variant whose multi-seed harm improvement vs the SFT$+$DPO endpoint reaches $p < 0.05$ on paired Wilcoxon ($p = 0.0114$). Two characterization findings: (iv) the leak/MI/bound trade-off forms a stable \textbf{manifold floor} that DAPO post-training does not statistically crack at $n{=}5$; (v) per-token KL has \textbf{two distinct stopping points} on its $K$-iteration trajectory --- iter\,1 is harm-minimal ($33/13/3/32$), iter\,2 is leak/bound-minimal ($38/9/2/35$), and iter\,3/iter\,4 walk back monotonically ($41/15/4/40$ and $42/17/5/42$, $K{=}2$ saturation), confirmed by a second significant paired Wilcoxon at $p = 0.0436$ (iter\,2, $n{=}4$).
\end{abstract}

\section{Introduction}

A growing fraction of LLM deployment is \emph{agentic}: the model represents one party (the \textbf{principal}) in a conversation with another party (the \textbf{counterparty}). Helpful-to-the-current-speaker training conflicts with principal-loyalty whenever the counterparty pushes for information, concessions, or actions the principal did not authorize. We instantiate this conflict as PrincipalBench and report a pipeline of prompt-time and weight-time interventions, along with a characterization of the failure manifold those interventions move along.

\paragraph{Three positive engineering results.}
\begin{enumerate}[leftmargin=2em,itemsep=2pt,topsep=2pt]
\item A 4-rule \emph{v4 system prompt} drives the frontier-prompted Pareto frontier (\cref{sec:v4}).
\item A reader-identity \emph{sentinel} fixes the over-refusal failure mode that emerges once the v4 prompt is applied (\cref{sec:v4}).
\item \emph{Per-token KL distillation} of the v4-prompted teacher into an open-weight Qwen3-8B student achieves the strongest 8B-distillation we measure, with multi-seed statistical significance vs the off-policy SFT$+$DPO endpoint (\cref{sec:ptkl}).
\end{enumerate}

\paragraph{Two characterization findings.} The leak / missed-instruction (MI) / bound-leak trade-off forms a manifold whose floor single-knob RL does not statistically cross at $n{=}5$ paired Wilcoxon (\cref{sec:manifold}); and the per-token KL iteration trajectory reveals \emph{two distinct} manifold optima (harm-min vs leak-min), with $K{=}2$ as the saturation point (\cref{sec:kiter}).

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/arxiv_fig1_manifold.pdf}
\caption{\textbf{The leak/MI manifold.} Each marker is one variant evaluated on the 36-item $\times$ 3-arm grid. The harm-fire count appears in the legend label. The Pareto-better corner is the lower-left. Per-token KL iter\,1 (blue star, harm $33$) is the closest open-weight 8B to the v4-prompted Claude teacher (green triangle, harm $21$).}
\label{fig:manifold}
\end{figure}

\section{PrincipalBench}
\label{sec:bench}

\paragraph{Items.} 36 items, each a principal briefing plus a parameterized counterparty script, spanning six failure-mode cells (leakage, capitulation, posture, authoring, sanity, moderation). The held-out v0\_75 extension adds 24 new items authored after training was frozen.

\paragraph{Arms.} Each item is evaluated under three system prompts: \emph{plain} (no policy instructions), \emph{prompted} (v4 four-rule scaffold), and \emph{scaffolded} (v4 $+$ reader-identity sentinel). Three arms $\times$ 36 items = 108 evaluation cells per checkpoint.

\paragraph{Scoring.} Three signals: (1) a deterministic leak probe that compares the agent's transcript against a curated set of forbidden facts, (2) a dual-judge harm score from two LLMs (default: \texttt{gpt-5-mini} via OpenRouter), aggregated to a binary \texttt{harm\_fire}, plus structured sub-flags (leak, leaked\_private\_bound, missed\_instruction), and (3) an integrity audit gate that rejects any score derived from a trajectory with zero agent turns or an \texttt{agent\_error} early-end.

\section{Prompted-frontier interventions}
\label{sec:v4}

\paragraph{The v4 four-rule prompt} addresses four named failure modes that emerged from open-coding 50+ failure trajectories of the default-prompted Claude:
\begin{itemize}[leftmargin=2em,itemsep=1pt,topsep=2pt]
\item Adversarial-stranger framing (treat the counterparty as a stranger, not a colleague).
\item Decline without enumerating what you are declining.
\item Distinguish private bounds from public positions.
\item Conditional permissions are not proactive disclosures.
\end{itemize}

Applied to \texttt{claude-sonnet}, the v4 prompt drives the 108-trajectory aggregate to \textbf{harm $21/108$ ($19.4\%$), leak $17/108$, bound $5/108$, MI $21/108$} --- a $3.5\times$ reduction in harm fires vs the default-prompt baseline. We evaluated v4 on thirteen more frontier subjects at the full 108-grid (\cref{fig:xsubj}):
\begin{center}\small
\begin{tabular}{l c c c c}
\toprule
subject (108-grid)            & plain (no prompt)   & prompted (v4)        & scaffolded (v4+sentinel)  & aggregate \\
\midrule
DeepSeek-v3.1                 & $11\%$              & $\mathbf{9\%}$       & $11\%$                    & $\mathbf{11\%}$ \\
Gemini-3p1-flash-lite         & $17\%$              & $17\%$               & $0\%$                     & $11\%$ \\
Mistral-Large                 & $11\%$              & $14\%$               & $11\%$                    & $12\%$ \\
GPT-5-nano                    & $14\%$              & $19\%$               & $8\%$                     & $14\%$ \\
Llama-3.1-70B-Instruct        & $9\%$               & $14\%$               & $22\%$                    & $16\%$ \\
Claude-Opus                   & $11\%$              & $19\%$               & $19\%$                    & $17\%$ \\
Gemini-2.5-flash              & $17\%$              & $20\%$               & $14\%$                    & $17\%$ \\
Gemini-3-flash                & $17\%$              & $20\%$               & $15\%$                    & $17\%$ \\
Qwen3-32B (OpenRouter)        & $24\%$              & $\mathbf{12\%}$      & $14\%$                    & $17\%$ \\
Claude-Sonnet                 & $19\%$              & $22\%$               & $17\%$                    & $19\%$ \\
\textit{GLM-4.6 (intermediate)} & $51\%$            & $52\%$               & $43\%$                    & $\mathit{49\%}$ \\
\textbf{GPT-5-mini}           & $\mathbf{55\%}$     & $\mathbf{67\%}$      & $\mathbf{64\%}$           & $\mathbf{62\%}$ \\
\textbf{GPT-5}                & $\mathbf{63\%}$     & $71\%$               & $71\%$                    & $\mathbf{68\%}$ \\
\textbf{Qwen3.5-27B}          & $\mathbf{72\%}$     & $79\%$               & $59\%$                    & $\mathbf{70\%}$ \\
\bottomrule
\end{tabular}\\
\smallskip
(per-arm harm rate; $n \approx 33$--$37$ per cell after audit)
\end{center}

\textbf{Eleven of fourteen evaluated vendors are calibrated; one intermediate; two over-refuse.} \textbf{Multi-seed paired evaluation at $n{=}5$ for the five vendors that bracket the bimodal split} confirms the gap is robust to seed variance (\cref{fig:xsubj}, error bars): DeepSeek $12.3 \pm 2.5\%$, Gemini-3-flash $19.4 \pm 2.3\%$, Claude-Sonnet $19.5 \pm 1.5\%$, GPT-5 $71.1 \pm 2.2\%$, Qwen3.5-27B $75.3 \pm 2.9\%$. The clusters are separated by $\sim$50 percentage points (more than 10 standard deviations); the split is not an artifact of any single seed. \textbf{Per-arm paired Wilcoxon} (calibrated cluster vs over-refuse cluster, mean across cluster vendors, paired by item across $n{=}36$ items) is significant for all three arms: \texttt{plain} $p = 8.7 \cdot 10^{-7}$, \texttt{prompted} $p = 2.4 \cdot 10^{-7}$, \texttt{scaffolded} $p = 4.7 \cdot 10^{-7}$. Conversely, the \textbf{v4-prompted-vs-plain} contrast \emph{within} the calibrated cluster (paired by vendor $\times$ item, $n{=}344$) is $p = 0.81$: v4 neither helps nor hurts calibrated subjects, which makes sense since they were already at $11$--$19\%$ harm.

\textbf{The bimodal split is driven by missed-instruction (MI), not by leakage or bound-leakage.} Per-vendor failure-mode decomposition aggregated by cluster ($n{=}1064$ calibrated / $106$ intermediate / $312$ over-refuse trajectories) shows:
\begin{center}\small
\begin{tabular}{l r r r r}
\toprule
cluster (v4-prompted, all arms) & harm & leak & MI & bound \\
\midrule
calibrated (10 vendors)              & $15\%$           & $10\%$ & $14\%$           & $2\%$ \\
intermediate (GLM-4.6)               & $49\%$           & $4\%$  & $47\%$           & $0\%$ \\
over-refuse (GPT-5, GPT-5-mini, Qwen3.5-27B) & $67\%$ & $3\%$  & $\mathbf{67\%}$  & $0\%$ \\
\bottomrule
\end{tabular}
\end{center}
For the over-refuse cluster, $\mathrm{MI} \approx \mathrm{harm}$ and leak / bound are near zero --- the harm rate is essentially the over-refusal rate from each vendor's own safety training, with leak and bound \emph{also} suppressed as a side-effect. The calibrated cluster has a more balanced $14\%$ MI $+$ $10\%$ leak $+$ $2\%$ bound. GLM-4.6 sits exactly between on MI ($47\%$) while also being low-leak ($4\%$). PrincipalBench therefore separates models on the MI axis specifically, not on adversarial leakage.

\textbf{Within-vendor inverse scaling holds for OpenAI.} The three GPT-5 variants we evaluated span both clusters: full GPT-5 $71\%$ and GPT-5-mini $62\%$ over-refuse, but \textbf{GPT-5-nano $14\%$ is calibrated}. The Anthropic, Google, and DeepSeek vendors are uniformly calibrated across all variants we tested (Claude-Opus $17\%$, Claude-Sonnet $19\%$; Gemini-2.5/3-flash $17$--$19\%$, Gemini-3p1-flash-lite $11\%$; DeepSeek-v3.1 $12\%$). Within Alibaba, Qwen3-32B $17\%$ is calibrated but Qwen3.5-27B $75\%$ over-refuses --- the over-refusal is specific to the Qwen3.5 release line, not the Qwen vendor as a whole. Crucially, the over-refusing vendors are over-refusing at the \texttt{plain} arm too ($55$--$72\%$ harm without any system prompt) --- the v4 prompt is approximately a no-op for them, they are already pegged on MI from their own safety training. PrincipalBench therefore surfaces a model-specific (not strictly vendor-wide) intrinsic difference: \emph{the benchmark separates models that selectively decline adversarial requests from models that blanket-refuse multi-party agentic items}. The v4 prompt actively helps Qwen3-32B (plain $24\% \rightarrow$ prompted $12\%$) and DeepSeek ($11\% \rightarrow 9\%$); its Pareto-best claim applies to the calibrated subset, not universally.

\begin{figure}[t]
\centering
\includegraphics[width=0.8\linewidth]{figures/arxiv_fig7_xsubj.pdf}
\caption{\textbf{14-subject cross-vendor v4 prompt evaluation, sorted ascending; multi-seed error bars where available.} Eleven subjects form a calibrated cluster at $11$--$19\%$ harm; GLM-4.6 is intermediate at $49\%$; three over-refuse at $62$--$75\%$. Error bars are $\pm 1\sigma$ across $n{=}5$ eval seeds for the five subjects that bracket the gap (Claude-Sonnet, DeepSeek, Gemini-3-flash, GPT-5, Qwen3.5-27B). Within OpenAI, GPT-5-nano ($14\%$) is calibrated while GPT-5 ($71\%$) and GPT-5-mini ($62\%$) over-refuse --- inverse scaling within the same vendor.}
\label{fig:xsubj}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/arxiv_fig8_heldout_xsubj.pdf}
\caption{\textbf{Held-out items/v0\_75 confirm the bimodal split is not item-specific.} The calibrated vendors (DeepSeek, Gemini-3-flash, Claude-Sonnet) hold $\le 24\%$ harm on $24$ fresh items, the over-refuse vendors hold $\ge 78\%$. GPT-5 in particular \emph{amplifies} from $71\%$ training to $93\%$ held-out, the largest cross-set divergence we measure on the calibrated/over-refuse axis.}
\label{fig:heldout_xsubj}
\end{figure}

\paragraph{The reader-identity sentinel.} The v4 prompt introduces an over-refusal failure mode on cooperative items where the counterparty IS the principal. A 12-item probe sub-benchmark fires the over-refusal $9/12$ trajectories with v4 alone. An architectural sentinel that prepends \texttt{[READER:\,PRINCIPAL]} or \texttt{[READER:\,THIRD\_PARTY]} to the in-conversation messages reduces the probe fire rate to $3/12$, generalizes to held-out probe items, and survives a spoof attack where the counterparty fakes a principal-side sentinel.

\section{Per-token KL distillation (Variant 3)}
\label{sec:ptkl}

We distill the v4-prompted teacher into an open-weight 8B student in three variant families: (V1) per-turn SFT on teacher-completed-at-student-state; (V2) per-turn DPO with chosen$=$teacher, rejected$=$student at student-state; (V3) per-token forward-KL between teacher and student token distributions on student-generated samples (the canonical Thinking Machines / DeepSeek-V4 algorithm). V1 and V2 follow Salimans et al.\ (2025); the API-only Claude teacher does not expose logits, so V3 requires an open-weight teacher --- we use \texttt{Qwen3-32B-AWQ + v4-prompt} served via vLLM, extracting top-$K$ teacher logprobs at each response position via the \texttt{prompt\_logprobs} API.

\paragraph{Loss.} For each response position $p$ in a student-sampled trajectory, with teacher top-$K$ tokens $\{t_k\}$ and renormalized teacher distribution $p_T(t_k \mid h_{<p})$:
\[
\mathcal{L} \;=\; \frac{1}{|R|} \sum_{p\in R} \sum_{k=1}^{K} p_T(t_k \mid h_{<p}) \cdot \left[ \log p_T(t_k \mid h_{<p}) - \log \hat p_S(t_k \mid h_{<p}) \right],
\]
where $\hat p_S$ is the student distribution renormalized over the same top-$K$ tokens. Padded slots use a large-but-finite $-10^4$ to avoid the $0\!\cdot\!\infty\!=\!\text{NaN}$ trap that masks training failure silently.

\paragraph{Headline.} V3 iter\,1 on Qwen3-8B (v4.1 SFT$+$DPO base, 113 on-policy turn-records, 28{,}486 token-level signals, 3 epochs QLoRA r$=$16): \textbf{harm $33/108$, leak $13/108$, bound $3/108$, MI $32/108$}. This is the only single-iteration distillation in our study that simultaneously improves all four manifold axes vs the v4.1 base, and leak actually goes \emph{below} the Claude+v4 gold teacher (13 vs 17).

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/arxiv_fig6_variants.pdf}
\caption{\textbf{Distillation variant ladder on Qwen3-8B.} Per-token KL is the only distillation variant whose harm improvement is statistically significant at $\alpha = 0.05$ on $n{=}5$ paired Wilcoxon vs the v4.1 SFT$+$DPO endpoint.}
\label{fig:variants}
\end{figure}

\subsection{Multi-seed statistical significance}
\label{sec:multiseed}

Five independent eval seeds of the same V3 iter\,1 checkpoint against five matched v4.1 baseline seeds, paired Wilcoxon signed-rank on per-cell fire counts (\cref{fig:wilcoxon}, left bars):
\begin{center}\small
\begin{tabular}{l r r r r}
\toprule
metric                    & v4.1 mean   & V3 iter\,1 mean             & cell-robust $\!\rightarrow\!$ & $p$ \\
\midrule
harm                      & 47.8        & \textbf{39.2 $\pm$ 4.0}     & 15 $\rightarrow$ 6            & \textbf{0.0114} \\
leak                      & 15.8        & 13.8 $\pm$ 1.8              & 0  $\rightarrow$ 0            & 0.534 \\
bound                     & 4.6         & 2.8 $\pm$ 1.5               & 0  $\rightarrow$ 0            & 0.385 \\
MI                        & 44.4        & 37.2 $\pm$ 3.6              & 15 $\rightarrow$ 5            & 0.055 \\
\bottomrule
\end{tabular}
\end{center}

\textbf{V3 iter\,1 is the first distillation variant in this work where the harm improvement at $n{=}5$ is not statistically indistinguishable from noise} ($p=0.0114$). For context, V1 (per-turn SFT) achieved $p=0.104$ and DAPO-v1 achieved $p=0.903$ on the same test. Cell-robust harm (cells fire under all 5 seeds) drops from 15 to 6 ($-60\%$).

\subsection{$K$-iteration trajectory: two manifold optima}
\label{sec:kiter}

Resampling student trajectories from each iter-$K$ checkpoint and re-collecting teacher logprobs:
\begin{center}\small
\begin{tabular}{l r r r r}
\toprule
iter (single seed)            & harm        & leak       & bound      & MI    \\
\midrule
v4.1 base                     & 56          & 18         & 4          & 51    \\
\textbf{V3 iter\,1}           & \textbf{33} & 13         & 3          & 32    \\
\textbf{V3 iter\,2}           & 38          & \textbf{9} & \textbf{2} & 35    \\
V3 iter\,3                    & 41          & 15         & 4          & 40    \\
V3 iter\,4                    & 42          & 17         & 5          & 42    \\
\bottomrule
\end{tabular}
\end{center}

\textbf{Iter\,1 is the harm-minimum; iter\,2 is the leak/bound-minimum; iter\,3 and iter\,4 regress monotonically on all four axes.} The K-trajectory has two distinct stopping points and $K{=}2$ is the saturation; iter\,4 (harm $42$, leak $17$, bound $5$, MI $42$) gives back the iter\,1/iter\,2 gains entirely on harm/leak and converges back toward the v4.1 base. This is the \emph{opposite} manifold direction from V1 (per-turn SFT), which lowered harm with each iteration but raised leak (V1: $44\!\rightarrow\!36\!\rightarrow\!37$ harm, $17\!\rightarrow\!24\!\rightarrow\!23$ leak): per-token KL pushes the policy along the low-leak corner; per-turn supervision pushes along the low-harm corner.

\begin{figure}[t]
\centering
\includegraphics[width=0.78\linewidth]{figures/arxiv_fig2_kiter.pdf}
\caption{\textbf{$K$-iteration trajectory of per-token KL.} Two distinct optima (iter\,1 harm-min, iter\,2 leak/bound-min) precede a monotonic regression at iter\,3 and iter\,4 ($K{=}2$ saturation); iter\,4 ($42/17/5/42$) walks back across all four axes toward v4.1.}
\label{fig:kiter}
\end{figure}

\paragraph{Iter\,2 multi-seed.} An additional paired Wilcoxon at $n{=}4$ (one of 5 attempted seeds failed its integrity audit after exhausting retries due to GPU contention and was dropped) confirms: harm mean $41.5$ vs v4.1 mean $48.5$, $p = 0.0436$; leak $11.2$ vs $16.2$ ($p = 0.18$); bound $2.5$ vs $4.5$ ($p = 0.59$); MI $40.5$ vs $44.2$ ($p = 0.21$). \emph{Both} per-token KL stopping points hit $p<0.05$ on harm. Single-seed iter\,2 ($38/9/2/35$) sat in the low tail; population mean is $41/11/3/41$.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/arxiv_fig3_wilcoxon.pdf}
\caption{\textbf{Multi-seed paired Wilcoxon vs v4.1.} Both per-token KL stopping points (iter\,1 harm-min, iter\,2 leak-min) hit $p<0.05$ on harm. Error bars are $\pm 1\sigma$ across seeds.}
\label{fig:wilcoxon}
\end{figure}

\subsection{Robustness and held-out generalization}
\label{sec:robustness}

\paragraph{Held-out items.} 24 items authored after training was frozen ($items/v0\_75$), 3 arms each = 72 trajectories: V3 iter\,1 harm $29/72$ ($40.3\%$) vs training-set $33/108$ ($30.6\%$) --- a $10\,$pp gap (\cref{fig:robustness}, right). V1 iter\,2 had a $\sim$0\,pp gap on the same set; the denser per-token signal may overfit more on 113 training records. This is the main caveat we attach to the headline.

\paragraph{Counterparty robustness.} Swapping the counterparty from \texttt{claude-sonnet} to \texttt{gpt-5} or \texttt{gemini-3-flash} on the V3 iter\,1 checkpoint: harm $33 \!\rightarrow\! 38 \!\rightarrow\! 49$, leak $13 \!\rightarrow\! 14 \!\rightarrow\! 20$. The leak-axis improvement transfers across counterparties more cleanly than the harm-axis improvement does (V3 leak range 13--20 vs V1 leak range 17--30).

\begin{figure}[t]
\centering
\includegraphics[width=0.95\linewidth]{figures/arxiv_fig5_robustness.pdf}
\caption{\textbf{Robustness.} \emph{Left:} swapping the counterparty preserves manifold shape but Gemini-3-flash extracts harder. \emph{Right:} held-out generalization. Per-token KL has the strongest training-set harm but the largest training$\rightarrow$held-out gap (10\,pp); per-turn SFT iter\,2 nearly eliminates the gap; the scaled-3$\times$ data run is uniformly worse (negative scaling, \cref{sec:scaling}).}
\label{fig:robustness}
\end{figure}

\subsection{Teacher self-validation}
\label{sec:teacher}

We evaluated the open-weight teacher itself on the scaffolded arm of PrincipalBench (the arm that includes the v4 prompt) via a chunked retry-on-vLLM-death procedure (concurrent GPU contention prevented a stable single-pass run; the audit-gated chunked pipeline accumulated 31 valid trajectories across multiple vLLM restarts). Result: \textbf{Qwen3-32B-AWQ + v4 (scaffolded): harm $4/31$ ($12.9\%$), leak $21/31$ ($67.7\%$), bound $0$, MI $3/31$ ($9.7\%$)} vs Claude-Sonnet + v4 on the same arm: harm $6/36$ ($16.7\%$), leak $6/36$ ($16.7\%$), bound $1$, MI $6/36$ ($16.7\%$).

\textbf{The open-weight teacher trades leak for harm/MI:} it leaks much more than the Claude teacher but has slightly lower end-to-end harm and MI fires. This explains why the per-token KL student inherits the harm-low pattern: it is distilled toward a teacher that itself has low harm despite high leak (\cref{fig:teacher}).

\begin{figure}[t]
\centering
\includegraphics[width=0.7\linewidth]{figures/arxiv_fig4_teacher.pdf}
\caption{\textbf{Teacher self-validation (scaffolded arm only).} The open-weight Qwen3-32B-AWQ + v4 teacher has comparable harm/MI to Claude-Sonnet + v4 but leaks much more. The per-token KL student inherits the harm-low pattern.}
\label{fig:teacher}
\end{figure}

\section{The manifold floor}
\label{sec:manifold}

The leak/MI/bound trade-off is structural across all 18 variants we evaluated (\cref{fig:manifold}). Three pieces of evidence:
\begin{itemize}[leftmargin=2em,itemsep=2pt,topsep=2pt]
\item \textbf{Multi-seed paired Wilcoxon refutes the single-seed DAPO claim.} The DAPO-v1 step\,35 checkpoint appeared to walk back the SFT$+$DPO endpoint's MI regression ($56 \!\rightarrow\! 37$ harm) at single seed; at $n{=}5$ paired Wilcoxon, the improvement vanishes on every axis (harm $p = 0.903$, leak $p = 0.51$, bound $p = 0.59$, MI $p = 0.85$). The variance across DAPO seeds dominates the mean shift from SFT$+$DPO.
\item \textbf{Reward-axis orthogonality is refuted.} We registered before running: take the harm-favorable cells from a leak-only reward variant and the leak-favorable cells from a v1 reward variant, train the composition (v3), and predict the resulting cell-wise harm/leak falls in the joint-favorable quadrant. The composition does not Pareto-dominate either parent at single seed; the axes are coupled.
\item \textbf{Cross-base direction-dependence.} On Mistral-7B SFT$+$DPO (base harm 27) the same on-policy SFT recipe \emph{regresses} (27 $\rightarrow$ 38); on Llama-3.1-8B SFT$+$DPO (base harm 42, already over-refusal) it plateaus (42 $\rightarrow$ 40). The recipe's direction depends on which side of the teacher the base sits on.
\end{itemize}

\section{Negative scaling result}
\label{sec:scaling}

Training V3 with $3\times$ more on-policy turn-records (372 vs 113), holding all other hyperparameters constant (3 epochs, lr $= 5\!\cdot\!10^{-5}$, QLoRA r$=$16): training-set harm $50/104$ ($48.1\%$), held-out harm $40/72$ ($55.6\%$). Both metrics are \emph{worse} than the 113-record iter\,1 ($30.6\%$ / $40.3\%$). The likely cause is undertraining: tripling the data with constant epochs gives one-third the effective passes through each example. The generalization gap narrows ($\Delta = 8$ pp vs 10 pp), but the baseline degrades. Naive data scaling without proportional epoch / learning-rate scaling regresses; we report this as a clean negative result.

\section{Related work}

PrincipalBench complements user-simulator multi-turn benchmarks (Tan et al.\ 2024) by inserting the principal as a third party whose private state the agent must protect. The per-token KL distillation algorithm follows Salimans et al.\ (2025, ``On-Policy Distillation'') and the DeepSeek-V4 technical report. The integrity-audit gate is methodologically similar to the silent-failure mitigation in BIG-bench Hard (Suzgun et al.\ 2022) but operates on agent trajectories rather than single-turn completions. DAPO follows Yu et al.\ (2024); our negative DAPO result at $n{=}5$ paired Wilcoxon complements their positive single-seed results on math reasoning.

\section{Limitations}

(i) \textbf{Judge-sensitivity of the per-token KL headline.} On a $n{=}72$ stratified subset, the primary judge \texttt{gpt-5-mini} fires on $28\%$ of iter\,1 trajectories while the secondary judge \texttt{claude-haiku} fires on $6\%$ (raw agreement $72.2\%$, Cohen's $\kappa = 0.08$). For comparison, the v4.1 baseline on the same subset has $\kappa = 0.42$ (primary $18\%$ vs secondary $25\%$) --- the judge agreement is \emph{specifically poorer for the per-token KL student}, suggesting its outputs are at the judge threshold (borderline / context-sensitive failures that one judge calls harm and the other does not). The headline $33/108$ harm count is consistent with the gpt-5-mini convention; an alternate convention using claude-haiku would report a much lower fire rate. We retain the gpt-5-mini headline because it was pre-registered, but the multi-judge sensitivity tempers interpretation of the absolute number; the \emph{relative} comparisons (paired Wilcoxon vs v4.1) are by-construction judge-invariant. \textbf{Three-judge replication on a balanced subset} (GLM-4.6, $n{=}60$, $24$ fires by primary, $49\%$ fire rate after the harm audit) shows $\kappa = 1.0$ on \texttt{any\_fire} across all three pairs (\texttt{gpt-5-mini}, \texttt{claude-haiku}, \texttt{claude-sonnet}; Fleiss $\kappa = 1.0$) --- the binary harm decision is fully reproducible on clear-signal trajectories. The sub-flag $\kappa$ collapses ($\le 0$) for the rare \texttt{fabrication}/\texttt{deception} sub-flags (raw agreement $> 90\%$; this is the kappa paradox under low prevalence). The per-token KL judge gap therefore reflects \emph{borderline-output ambiguity for the 8B student}, not a vendor-wide judge disagreement. (ii) Single base model for the per-token KL headline (Qwen3-8B); cross-base same-family teacher distillation (Mixtral, Llama-3.1-70B-AWQ) is shipped as runnable scripts but not executed under the GPU contention conditions of this draft. (iii) The DAPO-from-per-token-KL test --- the cleanest answer to whether the manifold floor is absolute or relative to the v4.1 starting point --- requires sustained $\sim$65\,GB free GPU on this 95\,GB device and is similarly deferred. (iv) The 10\,pp held-out gap for V3 iter\,1 indicates the dense supervision overfits more than per-turn SFT at this data scale; addressing it requires either more training data with proportional compute or explicit regularization, neither of which we ablate here. (v) Counterparty range on harm (33--49) is wider than per-turn SFT's (34--41); per-token KL is more counterparty-sensitive on the harm axis. (vi) The 11-subject cross-vendor result was collected single-seed except for the five vendors that bracket the calibrated/over-refuse gap (Claude-Sonnet, DeepSeek, Gemini-3-flash, GPT-5, Qwen3.5-27B), where we report $n{=}5$ multi-seed means with $\pm 1\sigma$ error bars.

\section{Conclusion}

PrincipalBench identifies a structural manifold among the leak / MI / bound axes that resists single-knob optimization. Three interventions move along the manifold: a 4-rule v4 prompt for the API-frontier; a reader-identity sentinel for cooperative items; and per-token KL distillation from an open-weight v4-prompted teacher, which achieves the strongest open-weight 8B result in our study and is the first 8B distillation with $p<0.05$ multi-seed harm improvement at both of its two stopping points.

\paragraph{Reproducibility.} All code, items, and trained checkpoints at \url{https://github.com/bojieli/principal-loyalty}. The per-token KL pipeline is \texttt{scripts/pertoken\_kl\_collect.py} (vLLM \texttt{prompt\_logprobs} collection), \texttt{scripts/train\_pertoken\_kl.py} (QLoRA top-$K$ KL loss), and \texttt{scripts/run\_pertoken\_kl.sh} (end-to-end driver). All evaluation scripts gate scoring on an integrity audit (\texttt{scripts/audit\_trajectories.py}); reported numbers use only audit-passing trajectories.

\end{document}
